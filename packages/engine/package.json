{
  "name": "@aadesipo/engine",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Pure TypeScript, deterministic game engine for AadesiPo. No React, no DOM, no I/O.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "esbuild": "^0.28.1",
    "fast-check": "^3.22.0",
    "typescript": "~6.0.3",
    "vitest": "^4.1.9"
  }
}
